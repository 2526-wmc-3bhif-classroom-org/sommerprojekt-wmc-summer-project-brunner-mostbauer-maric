import express from "express";
import type { Response } from "express";
import { StatusCodes } from "http-status-codes";
import { ProgramService } from "./program_service.js";
import { type AuthRequest, isAuthenticated } from "../middleware/auth_handlers.js";

const router = express.Router();

// This swagger comment was generated by gemini
/**
 * @swagger
 * /api/programs:
 *   post:
 *     summary: Create a new license program
 *     tags: [Programs]
 *     security:
 *       - bearerAuth: []
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             type: object
 *             required:
 *               - DrivingSchoolId
 *               - LicenseTypeId
 *               - DateFrom
 *               - DateTo
 *               - Price
 *               - MaxParticipants
 *             properties:
 *               DrivingSchoolId:
 *                 type: integer
 *               LicenseTypeId:
 *                 type: integer
 *               DateFrom:
 *                 type: string
 *               DateTo:
 *                 type: string
 *               Weekdays:
 *                 type: string
 *               IsSchnellkurs:
 *                 type: integer
 *               Price:
 *                 type: number
 *               MaxParticipants:
 *                 type: integer
 *     responses:
 *       201:
 *         description: Program created successfully
 *       400:
 *         description: Invalid input
 *       401:
 *         description: Unauthorized
 *       403:
 *         description: Forbidden (Only schools or admins)
 */
router.post("/", isAuthenticated, async (req: AuthRequest, res: Response) => {
  const result = ProgramService.Instance.createProgram(req.body, req.payload!.user.Role);
  if (result.error) {
    res.status(result.status).json({ error: result.error });
  } else {
    res.status(result.status).json({ data: result.data });
  }
});

// This swagger comment was generated by gemini
/**
 * @swagger
 * /api/programs/school/{schoolId}:
 *   get:
 *     summary: Get all programs for a specific driving school
 *     tags: [Programs]
 *     parameters:
 *       - in: path
 *         name: schoolId
 *         required: true
 *         schema:
 *           type: integer
 *     responses:
 *       200:
 *         description: List of programs
 */
router.get("/school/:schoolId", async (req, res) => {
  const schoolId = parseInt(req.params.schoolId);
  if (isNaN(schoolId)) {
    res.status(StatusCodes.BAD_REQUEST).json({ error: { message: "Invalid school ID" } });
    return;
  }
  const result = ProgramService.Instance.getProgramsBySchool(schoolId);
  res.status(result.status).json({ data: result.data });
});

// This swagger comment was generated by gemini
/**
 * @swagger
 * /api/programs:
 *   get:
 *     summary: Get all license programs
 *     tags: [Programs]
 *     responses:
 *       200:
 *         description: List of all programs
 */
router.get("/", async (req, res) => {
  const result = ProgramService.Instance.getAllPrograms();
  res.status(result.status).json({ data: result.data });
});

export { router as programRouter };
